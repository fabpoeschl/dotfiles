# Repo files that are not dotfiles
IMPROVEMENTS.md
README.md
LICENSE
Brewfile
packages.txt
test/
vim/

# Tabby config (modified at runtime)
.tabby

# Neovim generated files (lazy.nvim lock file)
.config/nvim/lazy-lock.json

# Platform-specific: only deploy LaunchAgents on macOS, systemd on Linux
{{- if ne .chezmoi.os "darwin" }}
Library/
{{- end }}
{{- if ne .chezmoi.os "linux" }}
.config/systemd/
{{- end }}
